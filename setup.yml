name: spisonet
channels:
  - pytorch
  - nvidia
  - defaults

dependencies:
  - python=3.10
  - pytorch
  - pytorch-cuda=11.8
  - pip
  - pip:
    - scikit-image
    - matplotlib
    - numpy
    - mrcfile
    - fire
    - tqdm
    - .
